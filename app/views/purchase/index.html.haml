.purchase
  .purchase__logo
    = link_to image_tag('material/logo/logo.png', :width => '170', :height => '60'), root_path
  .purchase__confirm
    .purchase__confirm__title
      %p.message 購入内容確認
    .purchase__confirm--upper
      .purchase__confirm--upper__flex__content
        .purchase__confirm--upper__flex__content__image
          = image_tag "#{@item.images.first.src}"
        .purchase__confirm--upper__flex__content__list
          %ul
            %li.item__name
              = @item.name
            %li.item__price
              = @item.price
    .purchase__confirm--row
      .purchase__confirm--row__price
        .purchase__confirm--row__price--flex
          %p.message
            支払い金額
          %p.price__box
            = @item.price
      .purchase__confirm--row__payment
        %p.message
          支払い方法
        %p.message
          クレジットカードのみの決済となります。
      .purchase__confirm--row__address
        %p.message
          配送先（ご登録先住所への配送になります。）
        %p.address
          - if current_user.address.building.present?
            #{@city}#{current_user.address.local}#{current_user.address.block}#{current_user.address.building}
          - else
            #{@city}#{current_user.address.local}#{current_user.address.block}
      .purchase__confirm--row__submit
        = form_tag(action: :pay, method: :post) do
          %button.button 購入する
    .end__logo__container
      .end__logo__container__end__logo
        = link_to image_tag('material/logo/logo.png', :width => '170', :height => '60'), root_path
        %p :著作権:FURIMA.inc